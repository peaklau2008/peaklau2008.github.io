# Jekyll

## install Jekyll

```
sudo apt install rbenv
sudo apt install ruby-dev
rbenv install 3.1.2
gem install jekyll:3.9.4
gem install jekyll-remote-theme
gem install jekyll-seo-tag
gem install webrick
jekyll new my-jekyll-site

jekyll serve --host=0.0.0.0 --port 8080
```


